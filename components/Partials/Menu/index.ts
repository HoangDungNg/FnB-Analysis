import MenuItems from './MenuItems';
import SubTitle from './SubTitle';
import MenuWrapper from './Wrapper';

const Menu = {
  Wrapper: MenuWrapper,
  Items: MenuItems,
  SubTitle: SubTitle
};

export default Menu;
